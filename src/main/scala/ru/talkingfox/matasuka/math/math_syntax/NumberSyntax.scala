package ru.talkingfox.matasuka.math.math_syntax

import ru.talkingfox.matasuka.math._

trait NumberSyntax {
//  implicit class NumberMathOperationOps[A: Numeric](num: A) {
//    def *(other: MathExpression): Multiply =
//      Multiply(Number(Numeric[A].toDouble(num)) :: other :: Nil)
//    def -(other: MathExpression): Sum =
//      Sum(Number(Numeric[A].toDouble(num)) :: Minus(other) :: Nil)
//    def +(other: MathExpression): Sum =
//      Sum(Number(Numeric[A].toDouble(num)) :: other :: Nil)
//    def ^[B <: MathExpression](other: B): Power[Number, B] =
//      Power(Number(Numeric[A].toDouble(num)), other)
//  }
}
